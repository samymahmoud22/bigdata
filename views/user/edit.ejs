
<!DOCTYPE html>
<html>
<head>
  <title>Edit Student</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="light">
<div class="container mt-5">
  <div class="card p-4">
    <h3>Edit Student</h3>
    <form method="POST" action="/update/<%= user._id %>">
      <input class="form-control mb-2" name="name" value="<%= user.name %>" required>
      <input class="form-control mb-2" name="age" type="number" value="<%= user.age %>" required>
      <input class="form-control mb-2" name="phone" value="<%= user.phone %>" required>
      <input class="form-control mb-2" name="subject" value="<%= user.subject %>" required>
      <input class="form-control mb-2" name="date" value="<%= user.date %>" required>
      <button class="btn btn-primary">Update</button>
    </form>
  </div>
</div>
</body>
</html>
